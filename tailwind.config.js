export default {
  theme: {
    extend: {
      fontFamily: {
        sans: ["var(--font-inter)", "sans-serif"],
      },
      colors: {
        accent: "var(--accent)", // 👈 now you can use bg-accent
      },
    },
  },
};
